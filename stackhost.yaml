user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.10"

commands:
  build:
  docker:
    worker: Dockerfile
  package:
    - "nano inxi"
 run:
  worker: bash run.sh

env:
  - "ENVIRONMENT=production"" 



